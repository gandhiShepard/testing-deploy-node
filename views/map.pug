extends layout

block content
  .inner
    h2= title
    .map
      .autocomplete
        input.autocomplete__input(type="text" placeholder="Search fpr Anything" name="geolocate")
      #map
        p Loading map...